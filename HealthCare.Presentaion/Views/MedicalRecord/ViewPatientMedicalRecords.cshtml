@model List<MedicalRecordViewModel>
@{
    ViewData["Title"] = "ViewPatientMedicalRecords";
}



@* 
<h2>Patient's Medical Records</h2>

<table class="table">
    <thead>
        <tr>
            <th>Record ID</th>
            <th>Doctor Name</th>
            <th>Date</th>
            <th>Description</th>
            <th>Prescription</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var record in Model)
        {
            <tr>
                <td>@record.Id</td>
                <td>@record.DoctorName</td>
                <td>@record.AppointmentDate</td>
                <td>@record.Description</td>
                <td>@record.Prescription</td>
                <td>
                    <a asp-action="ViewMedicalRecord" asp-route-id="@record.Id">View</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@


<link rel="stylesheet" type="text/css" href="~/css/site.css" />

<h2 id="doch2">Doctor's Medical Records</h2>

<div class="records-container">
    @foreach (var record in Model)
    {
        <div class="record-card">
            <h3>@record.DoctorName</h3>
            <p><strong>Doctor:</strong> @record.DoctorName</p>
            <p><strong>Date:</strong> @record.AppointmentDate.ToString("MMMM dd, yyyy")</p>
            <p><strong>Description:</strong> @record.Description</p>
            <p><strong>Prescription:</strong> @record.Prescription</p>
            <a asp-action="Details" asp-route-id="@record.Id" class="details-button">View Details</a>
        </div>
    }
</div>
<style>
    #doch2{
        text-align:center;
        margin:20px;
    }
</style>
